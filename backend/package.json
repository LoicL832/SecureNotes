{
  "name": "securenotes-backend",
  "version": "1.0.0",
  "description": "SecureNotes Backend - Système de stockage sécurisé de notes avec réplication",
  "main": "src/server.js",
  "scripts": {
    "init": "node init.js",
    "postinstall": "node init.js",
    "start": "node src/server.js --port=3001 --name=server1 --peer=https://localhost:3002 & node src/server.js --port=3002 --name=server2 --peer=https://localhost:3001",
    "server1": "node src/server.js --port=3001 --name=server1 --peer=https://localhost:3002",
    "server2": "node src/server.js --port=3002 --name=server2 --peer=https://localhost:3001",
    "test": "NODE_ENV=test node tests/security-tests.js",
    "dev": "node src/server.js --port=3001 --name=server1"
  },
  "keywords": [
    "security",
    "notes",
    "replication",
    "rest-api"
  ],
  "author": "Groupe 6",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "selfsigned": "^5.5.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "axios": "^1.6.5"
  }
}
